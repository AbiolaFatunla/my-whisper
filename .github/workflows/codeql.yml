# CodeQL - GitHub's Native Static Application Security Testing (SAST)
# Detects security vulnerabilities and coding errors

name: CodeQL Analysis

on:
  push:
    branches:
      - main
      - '!dependabot/**'
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Mondays at 6:00 AM UTC
    - cron: '0 6 * * 1'

permissions:
  actions: read
  contents: read
  security-events: write
  pull-requests: read  # Required for Dependabot PRs

jobs:
  analyze:
    name: Analyze JavaScript/TypeScript
    runs-on: ubuntu-latest

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
          # Run extended security queries
          queries: +security-extended,security-and-quality

      # Autobuild attempts to build any compiled languages
      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      # Note: SARIF upload requires Code Scanning enabled (free for public repos)
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        continue-on-error: true  # Don't fail if Code Scanning not enabled
        with:
          category: "/language:javascript-typescript"
